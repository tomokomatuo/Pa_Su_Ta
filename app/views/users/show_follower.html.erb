<% @user.followers.each do |user| %>
    <table>
    <tr>
      <td>  <%= user.nickname %> </td>
      <td><%= render 'follow_form', user: user %></td>
      <td><%= link_to '詳細', user_path(@user) %></td>
    </tr>
    
    </table>
<% end %>